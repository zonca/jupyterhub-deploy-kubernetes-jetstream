singleuser:
  storage:
    extraVolumes:
        # Manila CephFS share mounted via CSI-backed PVC
        - name: manila-share
          persistentVolumeClaim:
            claimName: manila-cephfs
    extraVolumeMounts:
        - name: manila-share
          mountPath: /share
          readOnly: false
